#pragma once

//NOTE: REMOVE FIRST "/*" FOR READABILITY!

/*

malloc(sizeof(int))
  //Memory for 1 int
malloc(sizeof(int*))
  //Memory for 1 pointer to an int
(int*)malloc(sizeof(int))
  //Same as malloc(sizeof(int)), but result is explicitly casted to an int pointer





*/
